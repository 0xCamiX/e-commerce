# public/landing — T1 asset slots

Figma drops PNG or SVG here using these **exact filenames**. Until then, the `.svg` placeholders keep the pin/scrub layout working. Overlay (black blur at the bottom of the photo) is a **child layer in the component**, not baked into the file.

## Chrome cutout

| File | Use |
| --- | --- |
| `extractor-chrome.svg` | Placeholder slot |
| `extractor-chrome.png` | **Drop here** — spherical-blade extractor, chrome/aluminum, transparent or paper `#F3EFE6`. No collage. |

The live hero uses the inline `ExtractorVisual` until `extractor-chrome.png` is present.

## Tips (callout stills)

| File | Maps to scrub label |
| --- | --- |
| `tip-1.svg` / `.png` | Hogar en teja |
| `tip-2.svg` / `.png` | Bodega/local |
| `tip-3.svg` / `.png` | Tallas 24 / 31 / 39 |
| `tip-4.svg` / `.png` | Tipo hongo |

## Photo pack (full-bleed, 4:5)

Leave the **bottom third** a bit darker so caption type holds. Do not bake captions into the photo.

| File | Shot |
| --- | --- |
| `photo-hogar-teja.svg` / `.png` | Hogar, teja |
| `photo-bodega.svg` / `.png` | Bodega / local |
| `photo-losa.svg` / `.png` | Losa (tipo hongo / techo) |

## Out of scope for this folder

Checkout, inventory, prices, fake customer quotes, m³/h numbers.
